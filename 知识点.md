###### 1.vue2 和 vue3 有哪些不同？

```vue
① 双向数据绑定的原理不同，vue2 使用的是发布订阅者模式和
Object.defineProperty()数据劫持，vu3 是使用的 Proxy 数据代理和 Reflect 反射 api
② vue3 支持拥有多个根节点 ③ 所用 api 不同，vue3 使用组合 api ④定义变量的方法不同
⑤ 生命周期的不同，vue3 使用 setup 代替了 vue2 的 beforeCreate 和 created
```

###### 2. 如何去做首屏优化？

```vue
① 使用路由懒加载 ② 非首屏组件使用异步组件 ③ 首屏不重要的组件做延迟加载
④减小首屏上 js,css 等资源文件的大小 ⑤ 图片懒加载 ⑥ 使用精灵图请求
```

###### 3.哪些方法会改变原数组，哪些方法不会？

```vue
push ,pop ,shift ,unshift , splice ,sort 会改变原数组 join ,filter ,some,
reduce,map ,concat 不会改变原数组
```

###### 4. js 检验数据类型的有哪些？

```vue
**typeOf**:只能判断基本数据类型 **Object.property.toString.call()**
可以判断所有数据类型 **instance** 只能判断引用数据类型 **().construtor**
得到()的基类
```

###### 4. 闭包

```vue
**定义**:可以读取其他函数内部变量的函数。
**闭包的特点**：变量不会被垃圾回收机制回收，创建私有空间
**闭包的缺点**：闭包较多时会消耗内存，导致页面性能下降
**闭包的作用**：延长变量的生命周期，创建私有作用域
```

###### 5.事件委托

```vue
事件委托又叫事件代理，原理是利用事件冒泡的机制来实现，也就是说把子元素的事件绑定在他的父元素上。
```

###### 6.原型和原型链

```vue
原型是一个对象，是函数上的一个 prototype
属性，通过改函数实例化出来的对象可以继承原型上的所有属性和方法。
原型链:一个实例对象在调用属性和方法时，依此会从实例本身，构建函数原型，原型的原型上去查找，直到查找到
null，没有找到属性返回 undefined，方法报错，这样查找所形成的链条就叫做原型链。
```

###### 7.new 的时候都做了那些操作？

```vue
① 创建一个新的空对象 ② 将空对象的**proto**属性指向函数的原型 ③ 把构造函数的 this
绑定到新的空对象身上，并把参数传入 ④
根据构造函数的返回值类型判断，如果是值类型就返回新对象，如果是引用类型就返回这个引用类型
```

###### 8.js 中的继承

```vue
① 原型链继承 实现原理：子类的原型指向父类的实例
缺点：当原型链中有引用类型的值时，该引用类型的值将会被所有函数实例共享
创建子类是不能向父类的构造函数中传参 ② 构造函数继承
实现原理：在子类的构造函数中执行父类的构造函数，并为其绑定 this
缺点：无法获取到父类构造函数上的属性和方法 ③ 组合继承
实现原理：在子类的构造函数中执行父类的的构造函数，并为其绑定
this，把父类的原型赋值给子类的原型 ④ Class 类实现继承，Class 通过 extends
关键字实现继承
```

###### 9.ES6 的新特性

```vue
① 新增了块级作用域(let ,const) ② 新增了定义类的语法糖 ③ 新增了解构赋值 ④
新增了函数参数默认，箭头函数(不能作为构建函数，没有 arguments，不能使用
call,bind,apply 改变 this)
⑤Promise(解决回调地狱的问题，三种状态:pending，fulfilled,rejected) ⑥ 新增了
set(数据唯一性，值不重复)，map(map 的 key 不受限制)数据结构，symbol
数据类型(代表独一无二的值) ⑦ 新增了模块化(import，export) ⑧ 新增了数组 api
，(map,find,findIndex,filter,some,reduce)
```

###### 10.async 和 await

```vue
同步代码做异步操作，两者必须搭配使用，调用函数会返回 Promise
```

###### 11.call,bind,apply 的区别

```vue
bind，call 接收的是一个参数列表，apply 接收的是一个数组，
call和apply会立即执行，bind 是返回一个改变 this指向的函数，
```

###### 12.事件循环 js

```
是一个单线程的脚本语言，在执行任务时会先执行同步任务，然后取执行任务队列里的任务，执行宏任务时会先检查是否有微任务，有微任务会先执行所有的微任务，然后在执行宏任务，全部执行完成后等待主线程的调用，调用完成后在去任务队列里查看是否有异步任务，这样一个循环往复的过程就叫事件循环。
宏任务有：setTimeout,setInterval,Ajax 请求，UI 事件，requestAnimationFrame
微任务有：async/await,Promise.then，MutationObserver，Promise 回调函数
```

###### 13.get 和 post 的区别

```vue
①get 一般用来请求数据，post 一般是用来提交数据 ②get 是 url
路径传参，安全性比较低，参数长度有限制，post 是 body 传参，安全性比 get
高，参数长度没有限制 ③get 请求刷新浏览器或者浏览器回退没有影响，post
会重新请求一遍 ④get 请求会被浏览器缓存，post 不会
```

###### 14.Token 一般存放在哪里？各有什么不同？

```vue
① 储存在 localStorage 中，但是每次调用接口时都要把当做一个字段传给后台 ② 储存在
cookie 中，每次请求接口都会自动携带，但是不能跨域 ③ 存储在 sessionStorage
中，每次调用接口时要放在 HTTP 请求头的 Authorization 字段中
```

###### 15.浏览器的缓存策略

```vue
浏览器的缓存分 强缓存(本地缓存)，协商缓存(弱缓存)
强缓存：不发起请求，直接使用缓存里的内容，浏览器把 JS,Css,Image
等存到内存中，下次用户访问直接从浏览器内存中取，提高性能。
协商缓存：需要向后端发送请求，通过判断来决定是否使用协商缓存，如果请求内容没有变化，则返回
304，浏览器就用缓存里的内容。 强缓存的触发条件：http1.0:时间戳响应头
http1.1,Cache-control 响应头
弱缓存(协商缓存)触发条件：http1.0：if-modified-since 请求头，last-modified
响应头。 http1.1 触发条件：if-none-match 请求头，Etag 响应头。
```

###### 16.同源策略

```vue
http:(协议)//www(子域名).aaa(主域名).com:8080(端口号)/index/index.vue(资源路径)
同源策略是指协议，域名，端口号三者一致，否则会产生跨域。
跨域是可以发送请求，后端也会正常返回结果，只不过这个结果被浏览器拦截。
解决跨域的方法，jsonp,cors,websocket,反向代理
```

###### 17.JWT 令牌

```vue
JWT 全名 JSON WEB Token。有头部，有效载荷，签名三部分组成
```

###### 18.http 状态响应码

```vue
200：响应成功 302：页面或者请求进行了多次重定向 400：参数不正确 404：找不到请求
500：内部服务器错误(后端接口报错)
```

###### 19.进入组件会执行哪些生命周期?

```vue
beforeCreate created beforeMount mounted （组件加入keep-alive时会执行activated）
```

###### 20.进入子组件会依次执行哪些生命周期?

```vue
父 beforeCreate==> 父created==>父
beforeMount==>子beforeCreate==>子created==>子beforeMount==>子 mounted==>父
mounted
```

###### 21.如何在 created 中获取 dom ?

```vue
只要写异步代码就可以在created中获取dom，例如：settimeout,Promise.then(),nextTick
```

###### 22.发送请求是在 created 中还是 mounted 中？

```vue
如果请求的数据不需要在页面中展示，那么就在 created
中发送请求，如果需要展示，就在 mounted 中发送请求。
如果优先加载子组件中的数据，那么就在 mounted
中发送请求，如果优先加载父组件中的数据，就在 created 中发送请求。
```

###### 23.常用的生命周期使用场景？

```
created：1. 数据初始化（如默认值）2. 发送异步请求（不依赖DOM）3. 监听非模板相关的事件。
mounted：1.操作DOM（如初始化图表库）2. 发送需要依赖DOM的请求（如获取元素尺寸）3. 绑定原生事件。
beforeUnmount：清理工作：移除事件监听、取消定时器、销毁第三方库实例。
```

###### 24. vue 的通信方式？

```
父传子：props，ref
父传后代：Provide，inject
兄弟传值：全局事件总线，vuex
后代传父：子传父：$emit
```

###### 25.插槽（slot)

```
插槽分为匿名插槽，具名插槽，作用域插槽（可以传值的插槽）
```

###### 26. vueX 的属性有哪些？

```
 state：数据源
 getters：计算属性
 mutations：同步操作
 actions：异步操作
 modules：模块化

 vueX是单向数据流
```

###### 27.mutations 和 actions 的区别？

```
mutations：同步操作，不能包含异步操作，如果需要异步操作，需要使用 actions
actions：异步操作，可以包含异步操作，如果需要同步操作，需要使用 mutations
mutations是用来修改state的值的，actions是用来提交mutations的(通过commit提交mutations)
```

###### 28.vueX 是否是持久化存储？

```
不是，vueX是存在内存中的，刷新页面就会消失
```

###### 29.如何实现持久化存储？

```
1.使用localStorage，sessionStorage，cookie
2.使用vuex-persistedstate插件
```

###### 30.vueX 的使用场景？

```
用户登录状态、全局主题配置，购物车结算流程、多步骤表单
```

###### 31.路由的模式和区别？

```
路由有两种模式hash 模式和 history 模式，hash 模式是默认模式，history 模式需要后端配合。
 区别：
      hash 模式有#号，history 没有，hash 模式刷新页面不会向服务器发送请求，history 模式刷新页面会向服务器发送请求。
      找不到当前页面发送请求时，history会想后端发送一次请求，hash不会
      发送请求时Hash模式会向服务器发送#后面的内容，history会发送完整路径到服务器
      实现原理不同，Hash是window.location.hash，history是window.pushState

```

###### 32.路由导航守卫有哪些？

```
全局守卫：beforeEach，beforeResolve，afterEach
路由独享守卫：beforeEnter
组件内守卫：beforeRouteEnter，beforeRouteUpdate，beforeRouteLeave
```

###### 33. nextTick 的原理？

```vue
nextTick(fun){ return Promise.resolve().then(()=>{ fun() }) }
```

###### 34. v-if 和 v-show

```
v-if是通过创建销毁dom节点来实现显示与隐藏，v-show是通过修改css的display:none,block来实现
```

###### 35. css 的 scoped 的原理？

```
vue的css的scoped是通过给每个组件添加一个唯一的属性，然后通过属性选择器来实现样式的私有化
```

###### 36.scss 样式穿透

```
父元素类名 /deep/ 子元素类名
```

###### 37. computed 和 watch 的区别？

```
computed：计算属性，依赖其他属性值，并且computed的值有缓存，只有它依赖的属性值发生改变，下一次获取computed的值时才会重新计算computed的值。不能包含异步操作，必须有返回值
watch：监听属性，通过watch来监听某个属性值的变化，然后通过回调来执行相应的操作。可以处理异步操作，没有缓冲
```

###### 38.props, data ,methods ,watch, computed 的优先级

```
  props==>methods==>data==>computed==>watch
```

###### 39.vue 双向数据绑定原理？

```
vue双向数据绑定是通过数据劫持结合发布者-订阅者模式的方式来实现的，通过Object.defineProperty()来劫持data中各个属性的setter，getter，在数据变动时发布消息给订阅者，触发相应的监听回调。
```

###### 40.延迟加载 js 的方式？

```
defer：延迟执行，只支持IE
async：异步执行，不保证执行顺序，支持所有浏览器
<script async src="xxx.js"></script>
<script defer src="xxx.js"></script>
```

###### 41.null 和 undefined 的区别？

```
null表示一个空对象指针，转化成数值为0，undefined表示一个未定义的值转化为数值为NaN
```

###### 42.===和==的区别？

```
===：全等，值和类型都相等
==：相等，值相等，类型可以不相等
null==undefined ===>true
==在进行比较时会通过Object.prototype.valueOf()和Object.prototype.toString()来进行转换，然后进行比较，null和undefined的valueOf()和toString()返回的都是自身，所以==相等
```

###### 43. new 一个对象的过程？

```
1.创建一个空对象
2.将空对象的原型指向构造函数的prototype(将构造函数的原型赋值给空对象的原型)
3.将空对象赋值给函数内部的this（给构造函数绑定空对象的this，并执行）
4.执行构造函数内部的代码,并对返回值进行判断
function Person(name, age) {
  this.name = name;
    this.age = age;
    return 1;
}
function New(fun, ...args) {
  let obj= {}
  obj.__proto__=fun.prototype
  let res=fun.apply(obj,args)
  return res instanceof Object ? res : obj
}
```

###### 44. sort 给数组里的对象按某一属性排序

```
 let obj = [
        { ab: 5, b: "p" },
        { ab: 8, b: "k" },
        { ab: 18, b: "k" },
        { ab: 3, b: "h" },
        { ab: 88, b: "l" }
    ]
    let compare= (ab)=> {
        return (a,b)=>{

            let val = a['ab']
            let val2 = b['ab']
            return val2 - val
        }
        }
   const obj2 = obj.sort(compare("ab"))
    console.log(obj2)
```

###### 45.深拷贝

```
function deepClone(obj){
  //判断是数组还是对象
     let obj2= obj.constructor===Array?[]:{}
     //判断是引用类型还是值类型
     if(typeof obj!=="object"){
           return obj
     }
     for(let key in obj){
      if(typeof obj[key] !=="object"){
        obj2[key]=obj[key]
      }else{
        if(obj[key].constructor===Function){
          obj2[key]=obj[key].bind(obj2)
        }else{

          obj2[key]=deepClone(obj[key])
        }

      }
     }

     return obj2
}
```

###### 46. get 和 post 的区别？

```
 1.get一般用来获取数据，post一般用来提交数据。
 2.get请求数据参数会在url上，post是放在body中
 3.get请求参数有长度限制，post没有
 4.get请求参数会被浏览器缓存，post不会
 5.get请求参数会被保留在浏览器历史记录中，post不会
 6.get请求安全性低，post安全性高
```


